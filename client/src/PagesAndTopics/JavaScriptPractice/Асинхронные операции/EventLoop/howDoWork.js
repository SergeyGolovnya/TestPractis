
| пусто |

| recursive(1) |

| console.log("Помещаем в стек задачу 1") |
| recursive(1) |

| recursive(1) |

| recursive(2) |
| recursive(1) |

| console.log("Помещаем в стек задачу 2") |
| recursive(2) |
| recursive(1) |


| recursive(2) |
| recursive(1) |

| recursive(3) |
| recursive(2) |
| recursive(1) |

| console.log("Помещаем в стек задачу 3") |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| recursive(3) |
| recursive(2) |
| recursive(1) |

| recursive(4) |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| console.log("Помещаем в стек задачу 4") |
| recursive(4) |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| recursive(4) |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| console.log("Достигли конца") |
| recursive(4) |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| recursive(4) |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| recursive(4) |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| console.log("Возвращаем из стека 4") |
| recursive(4) |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| recursive(3) |
| recursive(2) |
| recursive(1) |

| recursive(3) |
| recursive(2) |
| recursive(1) |

| console.log("Возвращаем из стека 3") |
| recursive(3) |
| recursive(2) |
| recursive(1) |

| recursive(2) |
| recursive(1) |

| recursive(2) |
| recursive(1) |

| console.log("Возвращаем из стека 2") |
| recursive(2) |
| recursive(1) |

| recursive(1) |

| recursive(1) |

| console.log("Возвращаем из стека 1") |
| recursive(1) |

| пусто |
