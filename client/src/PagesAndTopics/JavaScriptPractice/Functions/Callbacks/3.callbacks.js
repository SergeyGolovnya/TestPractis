// ============ Демонстрация работы с callback-функциями на примере микроволновки ============
// Создаем функцию-микроволновку, которая принимает массив еды и callback-функцию
// для обработки каждого элемента массива
function microwave(food, callback) {
    return food.map(item => callback(item));
}

// ============ Подготовка исходных данных ============
// Создаем массив с замороженной едой для демонстрации
const food = ['🥶 замороженная пицца', '🥶 холодный суп', '🥶 вчерашние пельмени'];

// ============ Примеры использования callback-функций ============
// 1. Простой callback: только разогрев (замена эмодзи)
const heatedFood = microwave(food, item => item.replace('🥶', '♨️'));
console.log('Разогретая еда:', heatedFood); 
// ['♨️ замороженная пицца', '♨️ холодный суп', '♨️ вчерашние пельмени']

// 2. Callback с дополнительным форматированием: разогрев и добавление пожелания
const readyToServe = microwave(food, item => `♨️ ${item.slice(2)} - Приятного аппетита!`);
console.log('Готово к подаче:', readyToServe);
// ['♨️ замороженная пицца - Приятного аппетита!', ...]

// 3. Callback с описанием состояния: преобразование в информационное сообщение
const foodStatus = microwave(food, item => `${item.slice(2)} превратилась в горячее блюдо`);
console.log('Статус блюд:', foodStatus);
// ['замороженная пицца превратилась в горячее блюдо', ...]

// ============ Результаты выполнения ============
// Каждый callback демонстрирует разные способы обработки данных:
// 1. Простая замена значений
// 2. Форматирование с добавлением текста
// 3. Полное преобразование в новый формат