Here is a simple problem, implement useEffectOnce() as the name says itself, it runs an effect only once.


Тесты:
🤔 2 out of 2 tests failed to pass.

it runs effect once spec  , expects 1 but got 0

effect cleanup should also work spec  Expected spy cleanup to have been called once. It was called 0 times.